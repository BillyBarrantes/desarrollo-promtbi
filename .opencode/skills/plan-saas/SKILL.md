---
name: plan-saas
description: Diseña planes de trabajo para Next.js y FastAPI considerando validación determinista.
---

1. Analiza el requerimiento funcional.
2. Identifica si afecta al frontend (`/frontend`), al backend (`/backend`) o a ambos.
3. Define los criterios de aceptación y la suite de pruebas a ejecutar (`pytest` / `linter`).
